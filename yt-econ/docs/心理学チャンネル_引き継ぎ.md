# 現代人のための心理学 — 引き継ぎメモ（2026-09-25）

別セッションで続けるための現状まとめ。設計の詳細は `docs/2チャンネル目_心理学.md`。

## 決まっていること
- 名前: **現代人のための心理学**（YouTube 側のチャンネルは「心理学まくら」の名前で作成済み: https://www.youtube.com/channel/UCv-h9prBycfOLwu2ieAUcMw → YouTube Studio で名前の変更が必要）
- ながら聴き・本編 15〜20 分（4 章 × 2 択クイズ）・16:9・白地（デジタル庁デザインシステム準拠 `dads`）
- 四国めたんの声だけ（VOICEVOX 話者 2）。立ち絵なし・画面に字幕なし・字幕ファイル（CC）は付ける
- 画面を見ない人にも声だけで全部わかるように話す（選択肢の全文読み上げ・声のカウントダウン・答え合わせで選択肢を言い直す）
- 小学 5 年生に分かる言葉。専門用語は 1 回だけ言い換える
- テーマの軸: AI / コスパ・タイパ / 感情が爆発する人（「ヒステリー」という語は使わない） / SNS・スマホ / 仕事・職場 / 人間関係
- 本編 20:00 公開、Shorts 3 本は本編の公開後（21:30 / 7:30 / 12:30）
- サムネはユーザーが作る（`thumbnails/psych/ps_001_20260929.jpg` などに置く。無い日は自動サムネ）

## できていること（コード）
- `pipeline.format: honpen` で毎日の流れ: 台本（`honpen.py`）→ 本編（`quiz.build(wide=True)`）→ 20:00 予約 → 章から Shorts 3 本 → 本編の公開後に予約
- 概要欄: もくじ（YouTube チャプター）・今日のひとつ・参考・免責
- 初週の企画: `config/channels/psych/schedule.yaml`（9/25〜10/1 の 7 本）。9/25 の初回は手元で作って投稿。9/26 からは Actions（`pipeline.start_date: 2026-09-25`）
- Actions: `daily-upload.yml` の psych ジョブ。`PSY_YOUTUBE_REFRESH_TOKEN` が無い間は skip

## 残っていること
1. **ユーザー**: YouTube のチャンネル名を「現代人のための心理学」に変える（YouTube Studio → カスタマイズ → 基本情報）
2. **ユーザー + Claude**: 心理学チャンネル用の許可（refresh token）を取る
   - 経済チャンネルと同じ OAuth クライアント（`998066049516-…`、Google Cloud の My First Project）を使う。新しいプロジェクトは不要
   - 許可 URL は `scripts/auth_youtube.py` と同じスコープ（youtube.upload / youtube / youtube.force-ssl / yt-analytics.readonly）で作り、**ブランドアカウント（心理学チャンネル）を選んで**許可してもらう
   - `python scripts/auth_youtube.py --prefix PSY_ --code '<localhost の URL>'` で `.env` に `PSY_YOUTUBE_REFRESH_TOKEN` が入る
3. **ユーザー**: GitHub Secrets に `PSY_YOUTUBE_CLIENT_ID` / `PSY_YOUTUBE_CLIENT_SECRET`（経済と同じ値）と `PSY_YOUTUBE_REFRESH_TOKEN` を登録
4. **ユーザー**: BGM（mp3）を渡す → `assets/bgm/` と素材 zip（`MATERIALS_URL`）に入れ、`render.bgm.file` を書き換える
5. **Claude**: 新しい形（ながら聴き 18 分・4 章）でサンプルを 1 本作って確認してもらう。長さが外れたら `honpen.part_chars_min/max` を調整
6. BGM は Roie Shpigler - Hush Move（`render.bgm.file`）。Actions で使うには素材 zip（MATERIALS_URL）に入れる

## 手元で試す
```bash
cd yt-econ
python -m ytecon --channel psych run -n 1 --no-upload     # 台本 → 本編 → Shorts（投稿しない）
```
VOICEVOX（http://127.0.0.1:50021）と Claude Code（`claude -p`）が必要。
