# --- コア ---
anthropic>=1.0.0
pydantic>=2.7
PyYAML>=6.0
python-dotenv>=1.0
requests>=2.32
feedparser>=6.0

# --- 動画合成 ---
# システムに ffmpeg があればそちらを優先。無ければこの同梱バイナリを使う
imageio-ffmpeg>=0.4.9

# --- 画像・図表・サムネ ---
Pillow>=10.3
matplotlib>=3.8

# --- YouTube アップロード ---
google-api-python-client>=2.130
google-auth>=2.29
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# --- 参照動画の分析（ytecon learn）---
# 参考にしたい動画の字幕とメタデータだけ取得する。動画本体は落とさない
yt-dlp>=2024.4.9

# --- 開発用 ---
pytest>=8.0
